---
name: Bug report
about: Something broken in DockZ
labels: bug
---

## What happened

## What I expected

## Steps to reproduce

1.

## Environment

- macOS version:
- Mac model (M1/M2/…):
- DockZ version (menu bar → About, or `git rev-parse --short HEAD`):
- Docker CLI: DockZ-managed or your own install?

## Logs

<!-- Both live in ~/.dockz/ (or your custom data folder). -->
- Relevant tail of `host.log` (VM lifecycle):
- Relevant tail of `console.log` (guest boot), if the VM is involved:
